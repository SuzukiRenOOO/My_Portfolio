module.exports = {
    // siteUrl: process.env.SITE_URL || '',
    // generateRobotsTxt: true,
    // generateIndexSitemap: false,
    // robotsTxtOptions: {
    //     policies: [{ userAgent: '*', allow: '/' }],
    // },
    siteUrl: 'https://suzukiren.dev', // 👈 Example — use your actual deployed site URL
    generateRobotsTxt: true,
};